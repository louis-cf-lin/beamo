- `npm run lint` will run `next lint --dir app` which specifies the `app` directory since it only runs for `pages`, `components`, and `lib` directories by default. See https://nextjs.org/docs/api-reference/cli#lint
